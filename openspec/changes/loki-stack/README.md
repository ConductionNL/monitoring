# loki-stack

Add Loki log aggregation to the existing monitoring repository as a new component, following the established component pattern (prometheus/, grafana/, alerting/). Includes Loki + Alloy for log collection, Grafana Loki datasource, log-based alerting to Slack via Grafana Alerting, ArgoCD Application, and SOPS secrets pattern.
