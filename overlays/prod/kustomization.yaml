apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
- ../../rules/coredns/rules-coredns.yaml
- ../../rules/pods/rules-createconfig.yaml
- ../../alerting/alertmanager.yaml
- ../../alerting/secret-alertmanager.sops.yaml

commonLabels:
  release: mon
