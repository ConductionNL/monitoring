# Grafana Keycloak OAuth – waarden uit Keycloak (niet committen)
# Je maakt hier geen nieuwe secrets: vul de client secret in die Keycloak al toont
# bij je client (realm commonground, client "grafana" → tab Credentials).
#
# Kopieer naar .env, vul GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET in, draai: ./scripts/grafana-oauth-secret.sh
# Zie grafana/README.md

GF_AUTH_GENERIC_OAUTH_CLIENT_ID=grafana
GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET=

# Nextcloud-namespaces voor het Grafana-dashboard "Nextcloud-omgevingen" (komma-gescheiden).
# Optioneel: als je dit zet en scripts/grafana-nextcloud-dashboard-apply.sh draait, wordt die lijst in het cluster toegepast.
NEXTCLOUD_NAMESPACES=nextcloud, nextcloud-data, nextcloud-llm
