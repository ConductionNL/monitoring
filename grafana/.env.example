# Grafana Keycloak OAuth – secrets (niet committen)
# Kopieer naar .env en vul in. Gebruik: ./scripts/grafana-oauth-secret.sh
# Zie grafana/README.md

GF_AUTH_GENERIC_OAUTH_CLIENT_ID=grafana
GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET=
